# ORB strategies

Keep Opening Range Breakout signal code here. Record the opening-range duration,
entry/exit rules, timezone, trading session, and forced-flat time in every run
config so apparently similar tests remain reproducible.

